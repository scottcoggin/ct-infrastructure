# n8n Environment Configuration
# Copy this file to .env and update with your actual values

# =============================================================================
# Domain Configuration
# =============================================================================
N8N_DOMAIN=n8n.homenet24.lan
N8N_PORT=5678

# =============================================================================
# PostgreSQL Configuration
# =============================================================================
# Root user credentials (used for database initialization)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_secure_postgres_password_here
POSTGRES_DB=n8n

# Non-root user credentials (used by n8n application)
POSTGRES_NON_ROOT_USER=n8n
POSTGRES_NON_ROOT_PASSWORD=your_secure_n8n_db_password_here

# =============================================================================
# n8n Security Configuration
# =============================================================================
# Encryption key - CRITICAL: Generate using: openssl rand -base64 32
# This key encrypts credentials stored in the database
# BACKUP THIS KEY - you cannot recover credentials without it
N8N_ENCRYPTION_KEY=your_encryption_key_here_generate_with_openssl

# Webhook URL - Used for external integrations and triggers
WEBHOOK_URL=http://n8n.homenet24.lan

# =============================================================================
# n8n Optional Configuration
# =============================================================================
# Timezone (default: America/New_York)
TZ=America/New_York

# Basic authentication (optional - adds extra security layer)
# Uncomment these lines and update docker-compose.yml if you want basic auth
# N8N_BASIC_AUTH_ACTIVE=true
# N8N_BASIC_AUTH_USER=admin
# N8N_BASIC_AUTH_PASSWORD=your_secure_basic_auth_password_here

# =============================================================================
# Important Notes
# =============================================================================
# 1. DO NOT commit the .env file to version control
# 2. Generate N8N_ENCRYPTION_KEY using: openssl rand -base64 32
# 3. BACKUP your N8N_ENCRYPTION_KEY - you cannot recover credentials without it
# 4. Change all default passwords before deployment
# 5. The WEBHOOK_URL should match your n8n domain for external integrations
# 6. If using Portainer, configure these variables in the Portainer UI instead

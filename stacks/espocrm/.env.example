# EspoCRM Environment Configuration
# Copy this file to .env and update with your actual values

# =============================================================================
# Domain Configuration
# =============================================================================
ESPOCRM_DOMAIN=espocrm.homenet24.lan
ESPOCRM_PORT=8080
ESPOCRM_SITE_URL=http://espocrm.homenet24.lan

# =============================================================================
# MySQL Configuration
# =============================================================================
# Root password (for database administration)
MYSQL_ROOT_PASSWORD=your_secure_root_password_here

# Application database credentials
MYSQL_DATABASE=espocrm
MYSQL_USER=espocrm
MYSQL_PASSWORD=your_secure_mysql_password_here

# =============================================================================
# EspoCRM Admin Configuration
# =============================================================================
# Admin account (only used on first initialization)
ESPOCRM_ADMIN_USERNAME=admin
ESPOCRM_ADMIN_PASSWORD=your_secure_admin_password_here

# =============================================================================
# System Configuration
# =============================================================================
# Timezone (default: America/New_York)
TZ=America/New_York

# =============================================================================
# Important Notes
# =============================================================================
# 1. DO NOT commit the .env file to version control
# 2. Change all default passwords before deployment
# 3. Admin credentials are only used during first-time setup
# 4. After first login, change the admin password in EspoCRM settings
# 5. The ESPOCRM_SITE_URL should match your domain for proper URL generation
# 6. If using Portainer, configure these variables in the Portainer UI instead

# =============================================================================
# Post-Deployment Configuration
# =============================================================================
# After deployment, configure the following in EspoCRM:
#
# 1. Email Integration (Proton Mail):
#    - Settings > Personal Email Accounts > Create Account
#    - SMTP: smtp.protonmail.ch:587 (STARTTLS)
#    - IMAP: Not available without Proton Mail Bridge
#    - Use SMTP tokens from Proton Mail settings (paid plan required)
#
# 2. n8n Integration:
#    - Install n8n node: npm install @traien/n8n-nodes-espocrm
#    - API credentials: Settings > API Users
#
# 3. Contact Import:
#    - Export contacts from LinkedIn as CSV
#    - Use n8n workflow for automated import and deduplication
